app:
  client:
    base-url: "http://localhost:5500"
  cors:
    allowed-origins:
      - "http://localhost:5500"
  template:
    storage-dir: "D:/p/test-data"

server:
  servlet:
    session:
      cookie:
        same-site: Lax
        secure: false
